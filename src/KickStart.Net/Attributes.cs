using System;

namespace KickStart.Net
{
    /// <summary>
    /// Indicates the attributed class, method, variable is made more public than required 
    /// purely for testing purpose
    /// </summary>
    public class VisibleForTesting : Attribute
    {
    }
}
